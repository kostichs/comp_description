# Команды для VM - v10e: Увеличена параллельность (качество сохранено)

# Остановка и удаление старых контейнеров
docker-compose down

# Удаление старых образов
docker system prune -a -f

# Пересборка всех сервисов
docker-compose build --no-cache

# Запуск сервисов
docker-compose up -d

# Проверка логов
docker-compose logs -f --tail=100

# Проверка здоровья
docker-compose ps

# === ИЗМЕНЕНИЯ в v10e ===
# 1. Увеличена параллельность с 5 до 12 компаний одновременно
# 2. Sync анализ сохранен (качество не ухудшено)
# 3. Последовательность продуктов сохранена
# 4. Ожидаемое улучшение скорости: 2-2.5x

# === Ожидаемая производительность ===
# - 5-10 компаний: ~15-20 минут (вместо 40)
# - 10-20 компаний: ~25-35 минут (вместо 80)
# - 20+ компаний: ~45-60 минут (вместо 120+)

# Для мониторинга обработки:
docker exec -it company-description-criteria_processor-1 tail -f /app/output/*/processing.log

echo "v10e готов - увеличена параллельность без потери качества!" 